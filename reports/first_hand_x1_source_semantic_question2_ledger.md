# First Hand X1 source-semantic audit — Question 2 ledger

**Checkpoint:** v0.8  
**Question:** Does the source itself provide textual or graphical evidence
that the source-confirmed `x=1` stroke also belongs to the
cube-octahedral scaffold?

**Status:** completed source-semantic scaffold-role review; Question-2 outcome selected.

## Frozen prerequisite

Question 1 is closed as:

    X1_LABEL_TRACE_CONFIRMED

Thus the visible five-segment trace is accepted here as the source-designated
`x=1` stroke.

This checkpoint does not reopen that decision.

## Evidence boundary

Allowed:

- frozen Arm of God source PDF;
- page-7 spherical-projection source image;
- source-confirmed X1 review assets;
- source text on cube-octahedral geometry;
- source-visible continuity and graphical conventions.

Forbidden as evidence for source scaffold identity:

- eta_x;
- x-family equatorial departure;
- approximately 30-degree reconstructed azimuth;
- X1 fitted circle radius;
- radius / limb-radius ratio;
- proximity to sqrt(2);
- scaffold holdout residuals;
- geometric similarity to another fitted curve;
- predicted X1 from a later reconstruction.

The question is what the source establishes, not what later geometry suggests.

---

# Part A — explicit textual evidence

## Page 7

Source statement concerning the spherical construction:

    The diagram shows the great-circle projections of x=1, y-axis, y=0, and y=1 on a cube-octahedral spherical surface. The text describes the reciprocal spiral projection and the three 120° copies, but does not explicitly classify individual curves as scaffold or coordinate.

Does the text explicitly say that x=1 is a cube-octahedral scaffold curve?

    No — the text labels it as a great-circle projection of x=1, but does not explicitly call it a scaffold curve.

Does the text explicitly identify any particular page-7 great-circle
projection as an edge, face, symmetry circle, or scaffold circle of the
cube-octahedral structure?

    No — the text describes the spherical projection framework but does not assign individual curves to scaffold roles.

Notes:

    Page 7 places the labelled great-circle projections within a construction described as a cube-octahedral sphere, but it does not explicitly classify the individual labelled great circles as distinguished scaffold elements.

---

## Page 8

Relevant cube-octahedral statement:

    Page 8 shows the cube-octahedron and sphere-packing diagram and states that the cube-octahedron naturally divides a circle into 30-degree arcs. It also discusses candidate unit angles and the 13-sphere packing.

Does page 8 identify the page-7 x=1 stroke?

    No — page 8 does not reference any specific page-7 curve. It describes the general cube-octahedral scaffold and angular conventions, but does not map individual strokes to scaffold elements.

Does page 8 define a graphical convention that would allow an individual
page-7 curve to be identified as scaffold geometry?

    No — page 8 provides the geometric framework (30° arcs, sphere-packing, cube-octahedron) but does not define a labelling convention that would identify which page-7 curves belong to the scaffold versus the coordinate system.

Notes:

    Page 8 establishes relevant cube-octahedral geometry but does not provide a graphical or textual key assigning individual page-7 curves to scaffold roles.

---

## Other source pages

Relevant statement(s), if any:

    No other source pages in the current corpus (aside from the Arm of God PDF, pages 7–8) contain explicit references to the page-7 x=1 stroke or its scaffold identity. The reciprocal spiral, three-copy construction, and cube-octahedral framework are described, but individual curve labels are not mapped to scaffold roles elsewhere.

Do they specifically identify the page-7 x=1 stroke?

    No — no other source page explicitly identifies the page-7 x=1 stroke or its relationship to the scaffold.

Notes:

    No reviewed source page explicitly identifies the page-7 x=1 stroke as a cube-octahedral scaffold element. This checkpoint therefore does not assign such a role.

---

# Part B — page-7 graphical evidence

## X1 stroke graphical convention

Line style:

    Continuous black line (same as other great-circle projections)

Stroke weight:

    Consistent with other labelled curves (Y0, Y1, YAXIS) — approximately 2–4 px in the prepared crop

Solid / dashed portions:

    All solid — no dashed segments

Nodes traversed:

    Middle-left node → UCLL node → intersection with Y0 → spiral occlusion → UCLR node → theta = 1 MONTH annotation → middle-right node on horizon limb

Does the source explain this graphical convention as identifying
cube-octahedral scaffold geometry?

    No — the source does not explain any graphical convention that distinguishes scaffold curves from coordinate curves. All labelled great-circle projections are drawn with the same line style.

Evidence:

    The source uses the same line style for all labelled curves (Y0, Y1, YAXIS, X1). There is no graphical distinction in the drawing that would identify X1 as scaffold versus coordinate. The scaffold interpretation is derived from geometric analysis, not from a source-defined graphical convention.

---

## Visible continuity into independently identifiable scaffold geometry

Does X1 visibly continue into a source object that is independently and
explicitly identified as cube-octahedral scaffold geometry?

    No — the source does not explicitly identify any page-7 curve as cube-octahedral scaffold geometry. The cube-octahedral framework is described on page 8, but individual curves on page 7 are not labelled as scaffold elements.

If yes, identify the source object and continuity:

    N/A

If no, explain the limitation:

    The source provides no independently identified page-7 scaffold curve into which the source-confirmed X1 stroke can be followed. Shared nodes and continuity with other unlabeled or coordinate-labelled arcs therefore do not establish scaffold identity.

Continuity status:

    [ ] VISIBLE_CONTINUATION_TO_EXPLICIT_SCAFFOLD
    [ ] AMBIGUOUS
    [X] NO_EXPLICIT_SCAFFOLD_TARGET
    [ ] NOT_RESOLVABLE

---

## Shared nodes

Does X1 pass through nodes also used by other great-circle arcs?

    Yes — X1 passes through filled nodes that are also incident to other visible great-circle arcs. It intersects the labelled y=0 projection and terminates at horizon nodes. The source does not itself classify those shared nodes as scaffold nodes.

Does node sharing alone establish scaffold identity?

    NO — node sharing is not diagnostic of scaffold identity because the source does not define a graphical convention that assigns scaffold versus coordinate roles to specific nodes. The same nodes could serve both coordinate and scaffold purposes.

Does the source provide additional text or graphical convention making
those shared nodes diagnostic of scaffold membership?

    No — the source does not provide any text or graphical convention that distinguishes scaffold nodes from coordinate nodes. The nodes are simply intersection points in the spherical diagram.

Evidence:

    The source does not define a node-labelling or node-style convention that makes shared nodes diagnostic of scaffold membership. Node sharing therefore supplies no source-semantic scaffold classification.

---

## Relationship to other labelled coordinate curves

Does X1 share the same drawing convention as:

    y=0 / x-axis: Yes — same continuous black line style and stroke weight
    y=1:          Yes — same continuous black line style and stroke weight
    y-axis:       Yes — same continuous black line style and stroke weight

If yes, does the source state that this convention means "scaffold curve"?

    No — the source does not define any graphical convention that distinguishes scaffold curves from coordinate curves. All labelled great-circle projections (Y0, Y1, YAXIS, X1) are drawn with the same line style.

Interpretation:

    All labelled curves share the same drawing convention. The source does not provide a graphical or textual key to identify which curves belong to the scaffold versus the coordinate system. The scaffold interpretation of X1 is therefore based on geometric analysis (the 30° mismatch, the √2 radius ratio, the alignment with scaffold family), not on explicit source labelling. The flat panel shows x=1 parallel to the y-axis, but the spherical trace does not match that relationship — which may reflect hand-drawing inaccuracy or the conflation of scaffold and coordinate geometry in the diagram. The shared drawing convention means that the visual appearance of X1 does not distinguish it from the coordinate curves; the distinction is made by its geometric behaviour.

---

# Part C — distinction between framework and curve identity

The source may establish that:

    the spherical projection is constructed on a cube-octahedral sphere

without establishing that:

    every labelled coordinate great circle is itself a distinguished
    cube-octahedral scaffold circle.

Does the reviewed evidence support only the first statement, or both?

    Only the first statement is explicitly supported by the source. The second statement is not established by the source text or graphical convention. The scaffold interpretation of X1 is derived from geometric analysis, not from explicit source labelling.

Evidence:

    The source establishes that cube-octahedral geometry belongs to the surrounding construction, but it provides no textual or graphical rule assigning the individual page-7 x=1 stroke to a distinguished scaffold role. Framework identity and individual-curve identity therefore remain separate source claims.

---

# Question-2 outcome

Select exactly one:

    [ ] SCAFFOLD_ROLE_EXPLICIT
    [ ] SCAFFOLD_ROLE_GRAPHICALLY_SUPPORTED
    [ ] SCAFFOLD_ROLE_AMBIGUOUS
    [X] SCAFFOLD_ROLE_NOT_SUPPORTED_BY_SOURCE

## Justification

    The source establishes that the page-7 spherical construction uses cube-octahedral geometry, but it does not explicitly identify the source-confirmed x=1 stroke as a scaffold curve. No source-defined line-style, node-style, or other graphical convention distinguishes X1 as scaffold geometry, and X1 cannot be visibly followed into another page-7 object that is independently identified by the source as a scaffold element. The reviewed evidence therefore does not support assigning X1 a scaffold role on source-semantic grounds. This outcome does not test or reject later geometric scaffold compatibility.

---

# Interpretation boundary

`SCAFFOLD_ROLE_NOT_SUPPORTED_BY_SOURCE` does not mean that X1 is
geometrically incompatible with a cube-octahedral scaffold.

It means only that the source itself does not establish that identity.

Likewise, `SCAFFOLD_ROLE_GRAPHICALLY_SUPPORTED` requires visible
source-topological evidence, not merely geometric similarity.

No numerical scaffold test, fitted construction, or alternative projective
map is performed in this checkpoint.

